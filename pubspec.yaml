name: grow_tracker_app
description: A grow tracking app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: any
  image_picker: any

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
